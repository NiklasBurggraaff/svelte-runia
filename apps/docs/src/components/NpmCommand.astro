---
import { Code, TabItem, Tabs } from "@astrojs/starlight/components";

interface Props {
    command: {
        type: "install";
        package: string;
    };
}

const { command } = Astro.props;

const npmCommand =
    command.type === "install" ? `npm install ${command.package}` : "UNKONWN COMMAND";
const pnpmCommand = command.type === "install" ? `pnpm add ${command.package}` : "UNKONWN COMMAND";
const yarnCommand = command.type === "install" ? `yarn add ${command.package}` : "UNKONWN COMMAND";
---

<Tabs class="tabs">
    <TabItem label="npm" icon="seti:npm">
        <Code code={npmCommand} lang="sh" frame="none" />
    </TabItem>
    <TabItem label="pnpm" icon="pnpm">
        <Code code={pnpmCommand} lang="sh" frame="none" />
    </TabItem>
    <TabItem label="yarn" icon="seti:yarn">
        <Code code={yarnCommand} lang="sh" frame="none" />
    </TabItem>
</Tabs>
